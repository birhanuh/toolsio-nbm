// Load factories
import directMessageFactory from "../factories/conversations/directMessage";

describe("directMessage", () => {
  beforeAll(done => {
    done();
  });

  afterAll(done => {
    done();
  });

  it("should fail with validation errors for each required field", done => {
    done();
  });

  it("saves directMessage", done => {
    done();
  });

  it("finds directMessage", done => {
    done();
  });

  it("updates directMessage", done => {
    done();
  });

  it("deletes directMessage", done => {
    done();
  });
});
