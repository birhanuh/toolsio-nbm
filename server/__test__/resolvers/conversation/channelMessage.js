// Load factories
import channelMessageFactory from "../factories/conversations/channelMessage";

describe("channelMessage", () => {
  beforeAll(done => {
    done();
  });

  afterAll(done => {
    done();
  });

  it("should fail with validation errors for each required field", done => {
    done();
  });

  it("saves channelMessage", done => {
    done();
  });

  it("finds channelMessage", done => {
    done();
  });

  it("updates channelMessage", done => {
    done();
  });

  it("deletes channelMessage", done => {
    done();
  });
});
