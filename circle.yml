machine:
  environment:
    XCODE_SCHEME: "test"
  xcode:
    version: "8.3.3"
test:
  override:
    - npm test toolsio-react/spec/

deployment:
  hub:
    branch: deploy
    commands:
      - chmod +x ./deploy-version.sh
      - ./deploy-version.sh

