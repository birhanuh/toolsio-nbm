machine:
  environment:
    XCODE_SCHEME: "test"
  xcode:
    version: "8.3.3"
test:
  override:
    - cd toolsio-react/spec/ 
    - npm test

deployment:
  hub:
    branch: deploy
    commands:
      - chmod +x ./deploy-version.sh
      - ./deploy-version.sh

