dependencies:
  override:
    - npm install client/ 
    - npm install server/  

test:
  override:
    - cd client/ && npm test 
    - cd server/ && npm test

deployment:
  production:
    branch: master
    commands:
      - ./deploy_prod.sh
  staging:
    branch: develop
    commands:
      - ./deploy_staging.sh




