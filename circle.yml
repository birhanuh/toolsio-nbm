machine:
  environment:
    XCODE_SCHEME: "test"
  xcode:
    version: "8.3.3"
dependencies:
  override:
    - npm install
test:
  override:
    - npm test spec/

deployment:
  hub:
    branch: deploy
    commands:
      - chmod +x ./deploy-version.sh
      - ./deploy-version.sh

